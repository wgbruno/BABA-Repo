import { StyleSheet } from "react-native";

export default StyleSheet.create({
    centerView: {
        flex:1,
        alignItems:'center',
        justifyContent:'center' 
    },
    emphasisText: {
        fontSize: 36,
        color:'blue',
        textAlign: 'center',
    }
 });